Pour utiliser le site en local depuis le téléchargement du site depuis github :
-dans un premier terminal :
    -cd frontend
    -npm init
-dans un second terminal :
    -cd backend
    -npm install express
    -npm install -g nodemon
    -nodemon server
-puis faire go live (grâce à l'extension live server)